# Backend Skeleton (CNPM)

## Hướng dẫn chạy project
1. Mở Terminal tại thư mục gốc.
2. Cài đặt các biến môi trường cho database (DB_URL, DB_USERNAME, DB_PASSWORD).
3. Chạy lệnh build: `mvn clean install`
4. Khởi động ứng dụng: `mvn spring-boot:run`